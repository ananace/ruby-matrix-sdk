---
sudo: false
language: ruby
rvm:
  - 2.4
  - 2.6
  - 2.7

cache:
  bundler: true
before_install:
  - gem install bundler rubocop
  - gem update --system
script:
  - rubocop lib/
  - bundle exec rake test
